# Research

The findings of each verification issue go here, one Markdown file per verification,
named for the question it answered.

They are written from runs of `Tools/hintjump-probe` (`just probe`) on a real machine,
so each file says which applications were read, on which macOS, and with which command.
